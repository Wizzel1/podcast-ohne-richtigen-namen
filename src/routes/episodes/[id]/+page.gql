query EpisodeById($id: Float!) {
	allEpisode(where: { episodeNumber: { eq: $id } }) {
		question
        answer
        answerLink
        spotifyId
		episodeNumber
        solvedBy {
			_id
			name
		}
	}
}
